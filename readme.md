# About Patagonia Theme
Patagonia theme is a Block Based theme using all the new fetures of WordPress and Gutenberg, all the styles are defined in the theme.json

# Develop
 - Run `npm i`
 - Run `npm run dev watch`